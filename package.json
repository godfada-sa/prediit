{
  "name": "prediit-static-app",
  "private": true,
  "version": "1.0.0",
  "description": "Static PREDIIT web application with Supabase migrations and Edge Functions",
  "scripts": {
    "build": "node scripts/validate.mjs",
    "check": "node scripts/validate.mjs",
    "test": "node --test tests/*.test.mjs",
    "preview": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
